The settings.xml in this dir contains server elements that I added for testing.

To populate the id element in the new XML file, we can grab it from settings.xml, or take as user input along with username and password.